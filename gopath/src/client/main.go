package main

import (
    "client/client"
    // "bufio"
    // "os"
    // "fmt"
)

func main()  {
    
    var vGlobal client.SGlobal
    vGlobal.Run()


    // r := bufio.NewReader(os.Stdin)
	// b, _, _ := r.ReadLine()
	// line := string(b)
    // fmt.Println(line)
}